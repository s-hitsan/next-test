import {Actor, Lusitana} from 'next/font/google';

export const actor = Actor({ subsets: ['latin'], weight: '400' });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
});